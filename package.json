{
  "name": "backend",
  "scripts": {
    "deploy:frontend-default": "firebase deploy --only hosting:nfireapp",
    "deploy:frontend-dashboard": "firebase deploy --only hosting:nfireapp-dashboard",
    "start": "run-p start:*",
    "start:firebase": "firebase emulators:start",
    "start:functions": "npm run watch --prefix functions"
  },
  "engines": {
    "node": "12"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "private": true
}
