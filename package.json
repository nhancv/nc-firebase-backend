{
  "name": "backend",
  "scripts": {
    "deploy:frontend1": "firebase deploy --only hosting:nfrontend1",
    "deploy:frontend2": "firebase deploy --only hosting:nfrontend2",
    "start": "run-p start:*",
    "start:firebase": "firebase emulators:start",
    "start:functions": "npm run watch --prefix functions"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "private": true
}
